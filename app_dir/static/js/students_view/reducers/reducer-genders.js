/*
 * The genders reducer will always return an array of genders no matter what
 * You need to return something, so if there are no gender then just return an empty array
 * */

export default function () {
    return [
        { "text": "Male", "id": "male"},
        { "text": "Female", "id": "female"},
    ]
}